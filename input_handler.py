class InputHandler():

    def jump():
        global air_bound
        air_bound = True

    def attack():
        keys = pygame.key.get_pressed()
        if keys[pygame.K_RIGHT]:
        elif keys[pygame.K_LEFT]:      
        else:


    def special_attack():
        if keys[pygame.K_RIGHT]:
        elif keys[pygame.K_LEFT]:      
        else:

    def movement():
        keys = pygame.key.get_pressed()
        if keys[pygame.K_RIGHT]:

        elif keys[pygame.K_LEFT]:
        

    def shielding():
        if keys[pygame.K_RIGHT]:
        elif keys[pygame.K_LEFT]:      
        else:

    def 
